Build the Windows version of ASM71

Build requirements:

cmake, Visual Studio 2017 build tools, Free Pascal compiler, nsis 3.0 with the
multi user extension (https://github.com/Drizin/NsisMultiUser).

Build:
run build_asm71.cmd compiles ASM71 and creates a windows installer for
the current build tool environment.
